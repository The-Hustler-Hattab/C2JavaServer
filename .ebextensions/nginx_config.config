commands:
  nginx_config:
    command: |
      # Use sed to insert the line inside the server block
      sed -i '/server {/a \    client_max_body_size 999M;' /etc/nginx/nginx.conf
      nginx -s reload
